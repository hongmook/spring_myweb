<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.khm.mapper.ReplyMapper">
  		
  		<insert id="insert">
  			insert into reply(seqno, content, id, board_seqno)
  						values(reply_seqno.nextval, #{comment}, #{id}, #{boardNo} )
  		</insert>
  		
  		<select id="getList" resultType="com.khm.dto.ReplyVO">
  	
  		<!-- 특수기호들을 인식하지 않게 함 -->
  		<!-- /*+INDEX(테이블이름 인덱스명)*/ 빠르게함?? -->
  		<![CDATA[ 
  			SELECT *
  			FROM(
	  			SELECT /*+INDEX(reply PK_REPLY)*/
	  				   rownum as rn, seqno, content, id, 
	  				   TO_CHAR(wdate, 'YYYY-MM-DD HH:MI:SS') as wdate
	  			FROM reply
	  			WHERE board_seqno = #{bno}
	  			  AND rownum <= #{cri.currentPage} * #{cri.rowPerPage}	    
  			) WHERE rn > (#{cri.currentPage}-1) * #{cri.rowPerPage}
  		]]>	
  		</select>
  		
  </mapper>